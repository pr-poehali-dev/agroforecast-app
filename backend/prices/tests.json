{
  "tests": [
    {
      "name": "GET prices — fallback always returns 5 crops",
      "method": "GET",
      "path": "/",
      "expectedStatus": 200,
      "expectedBody": {"any_live": false},
      "bodyMatcher": "partial"
    },
    {
      "name": "GET prices — force refresh",
      "method": "GET",
      "path": "/?force=1",
      "expectedStatus": 200,
      "expectedBody": {"any_live": false},
      "bodyMatcher": "partial"
    }
  ]
}